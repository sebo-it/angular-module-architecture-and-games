import { ShowPossibleMarkOnHoverDirective } from './show-possible-mark-on-hover.directive';

describe('ShowPossibleMarkOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPossibleMarkOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
